[package]
name = "eck-integrity"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Independent verifier for ECK tamper-evident event exports."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"

